% =====================================================================
% RESULTS: the 54-subject arms.  PRODUCED BY P1 + P2 -- see
% docs/unified_project_and_freeze.md S5.  Do not write prose here until
% both have run.
%
% P1  subsampling: draw many 9-subject cohorts from the 54 and report
%     detection rate vs n.  Pure numpy over existing per-subject CSVs.
%     This is what removes the task/channel/class confound from the
%     power argument -- the single highest-value item in the project.
%
% P2  random-init backbone seeds at n=54.  run_fm_probe.py:433 calls
%     set_all_seeds(args.seed) inside run_subject(), and main() loops
%     subjects at line 637, so every subject currently shares ONE random
%     backbone.  The headline control is one draw until this is fixed.
%
% Numbers available once REPORT.md is regenerated with Lee2019 folded in:
%   frozen probe   +0.044 acc, 39/54, p = 3.9e-4
%   fine-tuned     +0.086 acc, 46/54, p = 1.1e-8
%   pooled LOSO    +0.069 acc, 45/54, p = 7.3e-7
% Treat these as provisional until P2 lands.
% =====================================================================
